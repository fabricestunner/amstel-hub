{
  "name": "@amstel/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, enums and zod schemas used by web + api.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
